// src/app/ProductiveAuth.tsx
'use client'

// This component is now a no-op.
// We no longer read/write a Productive token from URL or localStorage.
// Server-side API routes use hard-coded credentials in productiveConfig.

export default function ProductiveAuth() {
  return null
}